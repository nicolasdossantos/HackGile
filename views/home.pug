extends layout
block content
    .container.ul.list-group
        each project, i in projects
            li.list-group-item
                a(href="#")=project.name
    .container.ul.list-group
          each project, i in projects
            li.list-group-item
                a(href="#")=project
